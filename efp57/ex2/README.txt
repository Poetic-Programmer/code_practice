exersize 2: Counting the number of characters

get user input and echo the input back with the number of characters
appended.

Constraints

 - output must contain original string
 - single output statement for output
 - use built-in functions to determine string length

additional

 - ensure user enters a value
 - create a gui version which updates the value of the string length as the user enters a character
