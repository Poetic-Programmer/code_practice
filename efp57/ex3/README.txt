Printing Quotes

use escape characters to show quotation marks in the user input. Have the user
enter a quote and an author, echo the input appropriately.

Additional

use a list to hold many instances of quotes and authors and echo them as
before.

Java

V1 :
  - create an input method (BufferedReader from InputStream)
  - output the input with a concatenated string

V2:
 - create a <map> to hold the data. key being the author, value being the
   quote. 
